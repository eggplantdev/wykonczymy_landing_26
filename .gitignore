# env — real secrets, never committed. .env.example is the tracked template.
.env
.env.*
!.env.example

node_modules/
.next/
# the e2e run's own dist dir — Next locks one dev server per dist dir, see next.config.ts
.next-e2e/
build/
dist/

.vercel
.DS_Store
*.log

# next / payload
next-env.d.ts
*.tsbuildinfo
/media

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.playwright-mcp/

dumps/

# page-board full-resolution captures
tools/page-board/.shots/

# page-board is a local audit tool — regenerate with `pnpm board`, never committed
public/page-board/

# slice-review-gate ledgers — local audit records, like the page-board captures above
.review-gate/
.env*
