// Shared so a section and the layout component it scrolls to cannot drift apart. Ids are not
// localized: they never reach the address bar the way a slug does.
export const CONTACT_FORM_ANCHOR = 'contact-form'
