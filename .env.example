# --- local dev database (docker compose up -d) ---
POSTGRES_DB=landing26
POSTGRES_USER=landing26
POSTGRES_PASSWORD=landing26
DOCKER_CONTAINER=landing26-cms
POSTGRES_URL=postgres://landing26:landing26@localhost:5436/landing26

# --- prod (Neon, via Vercel). Only used by db:dump to pull prod data down. ---
PROD_POSTGRES_URL=


PAYLOAD_SECRET=
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# --- media (Vercel Blob) ---
BLOB_READ_WRITE_TOKEN=

# --- email (nodemailer / SMTP) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# --- lead delivery into the leads app (yolo/wykonczymy) ---
LEADS_INTAKE_URL=
LEADS_INTAKE_SECRET=
