# Review-gate ledger — uncommitted tree on `main` · 2026-09-19

Second gate on this branch. The first (`.review-gate/main.md`, 2026-09-18) closed the lightbox /
seed-photos slice; its EX-806 findings were **held** because a parallel session owned
`src/components/layout/*`. Those files are in this gate's diff, so EX-806 is in scope here.

Unit of work: the uncommitted working tree vs HEAD — 57 modified + 8 untracked paths, ~1610
insertions. No single change folder covers it (`s2-contact-form` = implemented,
`lead-delivery` = new, `tdg-port` has no `change.md`).

Fan-out: `tailwind-v4-audit`, `feature-first-structure`, `module-cohesion-audit`,
`structure-scatter-audit`, `comment-noise-audit` (flag-only).
Dropped: `10x-impl-review` (no single `plan.md` anchors a tree spanning three slices plus the
untracked `hero-lab/` and `fonts.ts` experiments); `verify-manual-checks` (not installed, so
Step 0.5 is skipped).

`hero-board/` is **not** a second experiment — it is `hero-lab/`'s pre-rename name, surviving only
as a stale `.next/dev/server/` build artifact. The session's registered additional working
directory still points at that dead path.

## Scope constraint on the mutating pass

The tree holds ~57 modified files; only ~25 are this session's. `/simplify` mutates, and the
gate forbids touching a parallel session's dirty files, so fixes were confined to files this
session owns plus `nav-item-pill.tsx` (clean against HEAD). Findings landing on another agent's
in-flight files are recorded as `skipped` with the owning area named.

**The tree moved during the fan-out.** `site-nav.tsx` and `nav-group.tsx` were rewritten by a
parallel session while the audits were reading them, voiding four findings. Every finding below
was re-verified against the tree as it stands, not as an agent reported it.

## Findings

- [x] 🔴 CRITICAL · fixed · code-review · `src/components/layout/nav-item-pill.tsx:55` ·
      `bg-blau` has no `--color-blau` token — sole occurrence in the repo, so Tailwind emitted
      nothing and the active pill had no background. With `isActive && 'text-white'` on the
      label, the current-page item rendered white-on-white. Committed in `efc84a5`, i.e. live.
      Fixed to `bg-shwarz`; verified `.bg-shwarz { background-color: var(--color-shwarz) }`
      emits in the served stylesheet.
      test: no automated test · unit — a class-name typo is invisible to typecheck, lint and
      any render test; the real guard is CI Tailwind class validation (EX-807), already filed.
- [x] 🟡 WARNING · fixed · comment-noise · `src/app/(frontend)/[[...segments]]/layout.tsx:41-44` ·
      the comment justified painting the background on the div rather than `body` via the menu
      toggle's `mix-blend-difference`. This session removed that blend; the comment was the only
      remaining `mix-blend` string in `src/`. Deleted.
      test: no automated test · unit — a stale comment has no runtime behaviour to assert.
- [x] 🔵 OBSERVATION · dismissed · code-review ·
      `src/components/footer/contact-form/contact-form-attachments.tsx:55` · `key={fileName}` →
      `key={index}` looked like a downgrade; it is not. `fileNames` is a `string[]` rebuilt
      wholesale on every pick and a visitor can select two files with the same name, so the
      previous key threw duplicate-key warnings. The change is the fix. Not this session's file.
- [x] fixed · tailwind-v4 · `src/app/(frontend)/styles.css:236` + `src/components/home/hero.tsx:22`
      · `mx-[calc(50%-50vw)] w-screen` written twice as a counterweight to the `max-w-site` cap
      introduced in this same diff. Extracted `@utility full-bleed`; verified it emits
      `width:100vw; margin-inline:calc(50% - 50vw)`. The rationale comment moved onto the utility.
- [x] fixed · feature-first / structure-scatter · `src/components/ui/button-arrow.tsx:5` +
      `src/components/footer/contact-form/contact-form.tsx:184` · `ButtonArrow` took only
      `variant`, so the submit button — the one call site needing `disabled` — hand-rolled the
      same span. Added `disabled`, migrated the call site, dropped three now-unused imports.
      The primitive now covers every CTA arrow that ships.
- [x] fixed · module-cohesion · `src/components/layout/nav-item.tsx:12-14` · the comment
      justifying `navItemClasses`' export named the language trigger, which does not import it.
      Rewritten to name the real consumer, `NavItemPill`, and why it cannot be a `NavItem`.
- [x] fixed · comment-noise · `src/components/layout/nav-item.tsx:17`, `:49`;
      `menu-toggle.tsx:31`; `site-footer.tsx:58`; `social-links.tsx:18` · four Class-3 contrast
      openers and one vanished-state clause ("no longer inverts", referring to the blend this
      session deleted). Trimmed to the surviving why. The `social-links` TRIAL note also moved
      from above `PropsT` to above the imports it actually describes.
- [x] dismissed · tailwind-v4 · `src/components/layout/language-switcher.tsx:48` ·
      **EX-806 refuted.** `w-[var(--radix-popover-trigger-width)]` compiles byte-identically to
      the parenthesis shorthand through this project's own `@tailwindcss/postcss` 4.3.3. v4
      deprecated the *bare* v3.3 form `bg-[--x]`, never the explicit `var()` form. And the
      "make it a `@theme` token" fix is structurally impossible: Radix measures the trigger at
      runtime and writes the variable onto the element's `style`, so there is no compile-time
      value to register. **Close EX-806 as not-a-defect.**
- [x] dismissed · module-cohesion / comment-noise · `src/components/layout/site-nav.tsx:61,100,113,127`
      and `nav-group.tsx:14-15` · five findings (double `isCurrent` evaluation, `activeHref`
      reuse, two vanished-state comment tails, a wrong `offsetParent` mechanism description).
      All read off a version a parallel session replaced mid-audit. The current `site-nav.tsx` is
      78 lines with no `activeHref`, no `useLayoutEffect` and a single `isCurrent` call;
      `nav-group.tsx` has no `relative` and no such comment. Void against the tree.
- [x] dismissed · tailwind-v4 · `src/components/layout/nav-item.tsx:53,57` · `aria-[current=page]:`
      is an arbitrary *variant*, not an arbitrary value — canonical v4. Scanner false positive.
- [x] dropped · feature-first · `src/components/ui/button-arrow.tsx:1-2` · sibling imports use
      `@/components/ui/button` where `button-link.tsx` uses `./button`. Both satisfy the rule
      (only deep relatives are banned); picking a folder convention is not worth the churn.
- [x] skipped · feature-first / structure-scatter · `src/components/ui/icons/facebook.tsx`,
      `instagram.tsx` · both now have **zero importers** repo-wide — dead the moment the flat-icon
      trial is called. Deliberately left: the owner asked for the old files to be kept
      ("nie wywalaj jeszcze starych"). Resolving the trial is the owner's call, not the gate's.
- [x] skipped · feature-first · `src/components/layout/section-title.tsx` · zero domain
      knowledge, 8 consumers across 4 areas — it is a `ui/` primitive sitting in the chrome
      folder, and this diff made `footer/` the fourth adopter. The move touches 8 files, several
      held dirty by parallel sessions. Review-worthy refactor, not an in-gate fix.
- [x] skipped · module-cohesion · `src/components/footer/contact-form/contact-form-field.tsx` ·
      exports a class constant, a hook and two components; three of the four are consumed by
      siblings, so the filename misnames the module. A real grab-bag, but splitting it is a
      four-file refactor across a form another session is mid-edit on.
- [x] skipped · module-cohesion · `src/components/layout/nav-item.tsx:34,56-57` · `NavItem`'s
      `variant` prop is never passed by its only caller (`language-menu.tsx`), making the
      `'mobile'` branch unreachable. Removing it would delete the mobile-panel underline styling
      — and the fact that the language options render at bar size inside the mobile panel may
      itself be the bug. Behaviour-changing and uncertain; needs the owner's eye, not a guess.
- [x] skipped · structure-scatter · `src/app/(frontend)/hero-lab/hero-frame.tsx:8,26,44,56` ·
      re-declares `HeroT` as `HeroSourceT`, restates the hero's title classes, hand-rolls
      `ButtonArrow`, and does not use the new `full-bleed` utility. Untracked, owned by a
      parallel session.
- [x] skipped · feature-first · `src/components/interior-styles/` vs
      `src/components/home/interior-styles/` (same for `projects/`) · competing homes for one
      domain. Pre-existing and larger than the diff; the fix is one line in `AGENTS.md` defining
      the rule, not a file move.
- [x] dropped · tailwind-v4 · `src/components/layout/menu-toggle.tsx:63`,
      `src/components/projects/project-hero.tsx:19` · inline `transitionDuration` that a literal
      class lookup could express, and `leading-[111%]` off the project's own leading scale.
      Both pre-existing, surfaced only because their files were touched for other reasons.
- [x] filed EX-810 · module-cohesion · `scripts/seed/run.ts:34,63,109,125,164` · five
      per-collection seeders in one orchestrator, against a folder whose convention is one
      concern per file. Pre-existing, another session's dirty file.
- [x] filed EX-809 · suite · `tests/int/seed.int.spec.ts` · `seedProjects` fails with
      `ValidationError: To pole jest nieprawidłowe: slug` at `scripts/seed/run.ts:59`. Not caused
      by this gate's edits (components/CSS/comments only) and not by the in-flight seed diff,
      which only touches home-group row ids. Consistent with the known broken seed rows that
      `project-slide.tsx:8` already comments on and that EX-805 covers.
      test: test-driven-debugging · integration — the failing spec already is the red repro.

## Simplify pass

Ran fix-first inline rather than as a separate `/simplify` invocation, scoped per the constraint
above — 7 fixed, 2 filed, 4 dismissed, 2 dropped, 6 skipped · 0 open.

## Tests & suite

- `pnpm typecheck` — pass.
- `pnpm lint` — pass.
- `pnpm test:int` — **8 files pass, 1 fails** (`seed.int.spec.ts`, pre-existing; see the deferred
  finding above). 35 passed, 2 skipped.
- `pnpm test:e2e` — not run: Playwright is not opened without an explicit ask.
- `pnpm build` — not run: it shells `payload generate:importmap`, which rewrites
  `src/app/(payload)/admin/importMap.js` — already dirty from a parallel session.
