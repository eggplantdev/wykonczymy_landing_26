# CLAUDE.md — Claude Code entry point

**Everything lives in @AGENTS.md** — the canonical, complete onboarding file. The `@AGENTS.md`
reference above is a Claude Code import, so its full content is inlined here automatically; this
file has no content of its own.
